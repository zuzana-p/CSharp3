<label for=@For class="form-label text-muted small text-light">@Display</label>

@code {
    [Parameter] public required string For { get; set; }
    [Parameter] public required string Display { get; set; }
}